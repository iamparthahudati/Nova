# Dev scripts

- `setup.py` — placeholder for future repository bootstrap (venv, dependencies, permissions).
- `check_limits.py` — enforces Handbook §3.1 file/function size hard limits.
  Run from the repo root: `python scripts/dev/check_limits.py`. Runs in CI
  (`.github/workflows/backend-ci.yml`).
