# Database scripts

Placeholder scripts for schema migrations and data backups.

- `migrate.py` — future schema migration runner
- `backup.py` — future backup orchestration for `data/nova.db` and `data/nova_lancedb/`
