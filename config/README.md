# Config

Placeholder directories for future external configuration.

Prompt rendering currently lives in code at `apps/backend/services/brain/prompts.py`.
Personas, templates, and themes will be externalized in a dedicated milestone.
